## Alt Text Generator

Using Ruby and Amazon Bedrock

```
bundle exec ruby alt_text.rb \
  -s output/output.txt \
  -llm default \
  -d images \
  -p prompt.txt
```

Run this for help:

```
bundle exec ruby alt_text.rb -h
```
